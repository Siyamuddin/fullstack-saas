import { SessionsPage } from '../users/SessionsPage';

/** Admin sessions route — shared SessionsPage with admin variant */
export function AdminSessionsPage() {
  return <SessionsPage variant="admin" />;
}
