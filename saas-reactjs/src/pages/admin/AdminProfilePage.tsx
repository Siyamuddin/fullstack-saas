import { ProfilePage } from '../users/ProfilePage';

/** Admin profile route — shared ProfilePage with admin variant */
export function AdminProfilePage() {
  return <ProfilePage variant="admin" />;
}
